# 分支策略 — 补丁评审管道

## 为什么需要这个管道

在分布式 BIOS/固件开发工作流中，发送方和接收方团队之间的补丁交换通常使用
**原始 diff** — 即普通的 `git diff` 或 unified diff 输出，缺乏正确的提交元数据。
这些文件：

- ❌ 缺少 `From`/`Subject:` 头部 — `git am` 会直接拒绝
- ❌ 格式不统一 — 有些包含 `diff --git` 头部，有些是纯 `--- a/` 格式的 unified diff
- ❌ 版本之间存在重叠 — 多个 diff 文件修改相同的源文件，内容逐版演进
- ❌ 没有作者或日期信息 — 来源可追溯性丢失

**示例**（本项目的历史原始 diff 文件）：

| 文件 | 格式 | 有 `diff --git`? | 有 `From:`/`Subject:`? | 涉及文件数 |
|------|------|:----------------:|:---------------------:|:----------:|
| `0726.patch` | 普通 git diff | ✅ | ❌ | 6 |
| `IntelPatch20250528.diff` | 纯 unified diff | ❌ | ❌ | 4 |
| `IntelPatch20250626_GnrSp.diff` | 纯 unified diff | ❌ | ❌ | 4 |
| `Patch_0813.diff` | 普通 git diff | ✅ | ❌ | 8 |

管道标准化采用 **`git format-patch`** 输出来解决这些问题。所有传入的补丁必须
包含正确的提交头部，以确保可追溯性、自动化应用和冲突解决。

---

## 双分支策略

管道使用两个职责分明的分支：

```
                    发送方
                      │
                git format-patch
                      │
                      ▼
┌─────────────────────────────────────────────────┐
│           评审分支（临时/可丢弃）                │
│         review/<日期>/<补丁摘要>                  │
│                                                 │
│  • 传入补丁的着陆区域                           │
│  • 从基础分支新建                               │
│  • 通过 git am --3way 应用补丁                  │
│  • 冲突在此分支上解决（隔离）                   │
│  • 等价性检查在此执行                           │
│  • 集成完成后删除                               │
└──────────────────────┬──────────────────────────┘
                       │
               批准 + cherry-pick
                       │
                       ▼
┌─────────────────────────────────────────────────┐
│           工作分支（受保护）                     │
│         main  或  release/<名称>                │
│                                                 │
│  • 构建和发布分支                               │
│  • 仅接收已批准的提交                           │
│  • 不会被未审查的补丁污染                       │
│  • 为 CI/CD 保持干净的历史                      │
└─────────────────────────────────────────────────┘
```

### 评审分支 — `review/<日期>/<摘要>`

**用途：** 临时、隔离的补丁评估着陆区。

- 从配置的基础分支创建（例如 `release/bhs_pb2_35d44` 或 `main`）
- 命名规则：`review/2026-03-26/fix-dfe-gain`（日期 + 主题缩略）
- 通过 `git am --3way` 应用补丁，保留作者、日期和提交信息
- 如果发生冲突，**仅在此分支上**解决 — 工作分支在评审期间不受任何影响
- 集成完成后，评审分支可安全删除

### 工作分支 — `main` 或 `release/<名称>`

**用途：** 用于构建、测试和发布的稳定分支。

- 仅接收通过所有评审关卡的提交：
  1. ✅ 格式验证（步骤 1）
  2. ✅ 干净应用（步骤 2）
  3. ✅ 函数式等价性检查（步骤 3）
  4. ✅ 构建和测试通过（步骤 4）
  5. ✅ 发送方确认/批准（步骤 5）
- 提交通过 `git cherry-pick` 从评审分支进入
- 历史保持干净 — 没有来自补丁评审的合并提交

---

## 端到端流程

```
步骤1: 接收              步骤2: 应用              步骤3: 检查
──────────             ──────────              ──────────
format-patch   ──►  在 review/<日期>/<摘要>  ──►  比较发送方意图
格式验证             上执行 git am --3way         与接收方实际 diff
                    （基于基础分支）               按文件分类：
                                               MATCH/PARTIAL/MISMATCH

步骤4: 测试              步骤5: 集成
──────────             ──────────────
在评审分支上       ──►  发送方批准后
构建 + 单元测试          将每个提交 cherry-pick
                        从评审分支 → 工作分支
                        删除评审分支
```

---

## 分支配置

通过 `.patch-pipeline.toml` 或环境变量配置：

```toml
# .patch-pipeline.toml
release = "bhs_pb2_35d44"
base_branch = "release/bhs_pb2_35d44"    # 工作分支（构建/发布）
```

**工作分支的解析优先级：**

| 优先级 | 设置 | 示例 |
|:------:|------|------|
| 1 | `base_branch`（显式指定） | `release/bhs_pb2_35d44` |
| 2 | `working_branch`（非 `main` 时） | `develop` |
| 3 | `release/<release>`（设置了 release 时） | `release/bhs_pb2_35d44` |
| 4 | 默认值 | `main` |

---

## 为什么使用双分支？

| 关注点 | 评审分支 | 工作分支 |
|--------|:--------:|:--------:|
| 接收原始补丁 | ✅ | ❌ |
| 在此解决冲突 | ✅ | ❌ |
| 等价性检查目标 | ✅ | ❌ |
| 构建和发布 | ❌ | ✅ |
| 仅包含已批准的提交 | ❌ | ✅ |
| 干净的 git 历史 | 可丢弃 | ✅ |

**核心优势：** 如果补丁集存在问题 — 冲突、不匹配或测试失败 — 工作分支
**完全不受影响**。评审分支可以直接丢弃，团队可以使用修正后的补丁集重新尝试。

---

## 冲突解决

冲突在两个阶段处理：

### 应用阶段（步骤 2）
- `git am --3way` 使用共同祖先进行合并
- 如果发生冲突，管道暂停并报告哪个补丁失败
- 在评审分支上解决冲突，然后执行 `git am --continue`
- 工作分支不受影响

### 集成阶段（步骤 5）
- 从评审分支 `git cherry-pick` 到工作分支
- 如果发生冲突（在等价性检查后很少见），管道提供中止/继续选项
- 只有完全干净的 cherry-pick 才会进入工作分支
